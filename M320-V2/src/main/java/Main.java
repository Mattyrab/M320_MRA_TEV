/**
 * Entry point of the application to run the program
 */
public class Main {
    /**
     * Main method to start the program and display the menu.
     *
     * @param args Command-line arguments (not used).
     */
    public static void main(String[] args) {
        Menu menu = new Menu();
        menu.displayMenu();
    }
}
